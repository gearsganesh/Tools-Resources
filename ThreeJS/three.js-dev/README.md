# three.js

This folder contains the Three.js source tree used as a local development reference.

Stored source version: r185 / 0.185.0.

For production projects, prefer installing the published `three` package rather than modifying this reference copy. Keep project-specific experiments outside this folder.